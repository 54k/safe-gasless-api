# safe-gasless-api
```
It allows to create multisig wallet (gnosis safe) with opcode create2,
precumpute it's address and make transactions on 
behalf of the user with his approve and signature.
```

```
Use .env file to fill config parameters. 
Hardhat node must be enabled during local development.
```